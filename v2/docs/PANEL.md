# MeshSat control panel (PCB-C backer C7 under the face plate): software contract for the panel controller and the bridge

> Generation MESHSAT-830 (7 Sep 2026, `V2-SPEC.md`, appendix 32.52, 32.60). Nothing has been built: this contract describes the C7 backer as generated by `v2/ecad/tools/gen_sch_c.py` and is the specification for the panel controller firmware (MESHSAT-837) and for the bridge on the compute modules. The previous contract (the released C6 with the PCA9555 pair on the compute module's bus and the previous e-paper module on SPI0) is in the git history of this file.

Hardware record: `MESHSAT-709-geometry-appendix.md` sections 32.52 (fabric), 32.56 (face), 32.60 (C7 design). The panel is a **USB device**: an RP2040 panel controller (`U3`) on B16's slot-2 USB hub over the `USB_PNL` pair of the 2x13 ribbon `J_PANEL`. The compute modules never touch the panel's pins directly; every indicator, switch, the e-paper, the sounder and the light sensor are behind the controller, which presents one USB composite device (a CDC console for the bridge protocol and a vendor HID report for the state). The lines that must work without any software are hardware: MAIN PWR, EMCON, ZEROIZE and the TX lamp (section 6).

## 1. What sits on the backer

| Function | Parts (schematic references) | Owner |
|---|---|---|
| Panel controller | `U3` RP2040, `U4` W25Q16 QSPI flash, `Y1` 12 MHz, `R5`/`JP1` BOOTSEL strap, `TP1`..`TP3` SWD | firmware MESHSAT-837 |
| Indicators | 16 THT LEDs `D1`..`D16` through Mentor light guides in the plate, sinks on two PCA9555 (`U1` 0x22, `U2` 0x23), the TX lamp `D3` on `Q3` | controller |
| LED rail | `+5V` through the LIGHTING toggle `SW_LIGHT` (pole 1) to `LED_RAIL_SW`, `Q1` P-FET high side switched by `Q2` from `PANEL_PWM`, `RAIL_SENSE` divider | controller |
| Switches | `SW_MAIN` (19 mm, hardware lead to A22 `J_MAINSW`), `SW_PI` (16 mm, lead pads `PIJ2_A/B`), `SW_TEST` (16 mm, `TEST_SW`), `SW_SOS`, `SW_EMCON`, `SW_ZERO` (APEM locking toggles with hinged covers), `SW_LIGHT` (NKK DPDT ON-ON-ON) | mixed, section 4 |
| E-paper | Pervasive Displays E2370KS0C1 on the 24-way ZIF `J_EPD`, supply switch `Q5`, the boost stage `L1`/`Q6`/`D19`..`D21` per PDi's driving note rev 02 | controller |
| Sounder | `BZ1` IP67 panel sounder on `+5V` through `Q4` from `PWM1` | controller |
| Ambient light | `U_LIGHT` VEML7700 (I2C 0x10) under its own light guide | controller |
| EMCON logic | `U9` 74LVC1G04: `EMCON_HW` = NOT `TX_INHIBIT_n` | hardware |
| Pass-throughs | the Xenarc 709GNK monitor's connector block (its HDMI, USB touch and 12 V leads go below the plate to B16 and A22), two U-174/U headset jacks `J_HSJ1`/`J_HSJ2` (leads to D8 `J_HS1`/`J_HS2`), the USB camera module behind the sealed window (lead to B16 `J_CAM`) | not the panel's |

## 2. The ribbon `J_PANEL` (2x13, 2.54 mm; B16 `J_PANEL` carries the same map, checked by `check_contracts.py`)

| Pin | Net | Direction (seen from the panel) | Pin | Net | Direction |
|---|---|---|---|---|---|
| 1 | +5V | in (rail) | 14 | GND | |
| 2 | +5V | in | 15 | USB_PNL_P | USB 2.0 device pair to B16's slot-2 hub |
| 3 | GND | | 16 | USB_PNL_N | |
| 4 | SDA | the kit I2C bus, the controller is its master | 17 | GND | |
| 5 | SCL | | 18 | HB1 | in: slot 1 heartbeat |
| 6 | EXP_INT | out (the panel expanders' interrupt, also read by the controller) | 19 | HB2 | in: slot 2 heartbeat |
| 7 | TR_APRS | in: D8's real KEY line (lights the TX lamp) | 20 | HB3 | in: slot 3 heartbeat |
| 8 | EMCON_HW | out, hardware: high = transmitters inhibited | 21 | SLOT_EN1 | out: slot 1 rail enable |
| 9 | GND | | 22 | SLOT_EN2 | out |
| 10 | ZEROIZE_HW | out, hardware: low = wipe | 23 | SLOT_EN3 | out |
| 11 | TX_INHIBIT_n | out, hardware: low = inhibit (to D8's KEY gate) | 24 | PI_SHDN_REQ | out: clean shutdown request to the modules |
| 12 | HDMI_SEL1 | out: display switch input select | 25 | PI_KILL | out: hard power off request to A22 |
| 13 | HDMI_SEL2 | out | 26 | SHORE_INHIBIT | out: to A22 and the dock (charge inhibit) |

## 3. Controller pin map (RP2040 `U3`)

| GPIO | Net | Use |
|---|---|---|
| 0, 1 | SDA, SCL | the kit I2C bus (section 7), the controller is the master |
| 2, 3 | EPD_SCL, EPD_SDA | e-paper SPI (the iTC's 4-wire mode; MOSI only, the display's read path is unused) |
| 4, 5, 6, 7 | EPD_DC, EPD_CS, EPD_RST, EPD_BUSY | e-paper control; BUSY is high while the display refreshes |
| 8 | PANEL_PWM | LED rail dimmer through `Q2`/`Q1`, 2 kHz, 10 bit; boots low (rail dark until the firmware is up) |
| 9 | PWM1 | sounder through `Q4`; high = sound, PWM for patterns |
| 10, 11, 12 | HB1, HB2, HB3 | heartbeat inputs, one per compute slot (a slot toggles its line at 1 Hz while its supervisor runs) |
| 13, 14, 15 | SLOT_EN1..3 | slot rail enables (high = on), section 5 |
| 16, 17 | HDMI_SEL1, HDMI_SEL2 | the three-input display switch on B16: which slot drives the Xenarc |
| 18 | PI_SHDN_REQ | shutdown request to every module (high for 200 ms) |
| 19 | PI_KILL | hard kill to A22's power controller (held high 3 s) |
| 20 | SHORE_INHIBIT | charge inhibit to A22 and the dock, section 9 |
| 21 | EMCON_HW | read back of the hardware EMCON line |
| 22 | ZEROIZE_HW | read back of the ZEROIZE toggle (10k pull-up, 10 nF) |
| 23 | TR_APRS | read back of D8's KEY line (the TX lamp state) |
| 24 | EXP_INT | the expanders' interrupt, active low |
| 25 | LED_STAT | the controller's own status LED `D18` (on the board, not on the face) |
| 26 (ADC0) | RAIL_SENSE | `LED_RAIL_SW` through 10k: the LED rail is present when the LIGHTING toggle is not at BLACKOUT |
| 27 | TEST_SW | TEST / ACK button, pressed = low (10k pull-up, 10 nF) |
| 28 | SOS_SW | SOS locking toggle, closed = low (10k pull-up, 10 nF) |
| 29 | EPD_PWR_n | the e-paper's 3.3 V switch `Q5`, low = on |

Debounce 30 ms on every switch input; the two toggles are maintained switches (pull, flip, leave).

## 4. Expanders (PCA9555, on the controller's I2C bus, INT wire-ORed on `EXP_INT`)

`U1` at 0x22: port 0 = LED cathodes (sinks), port 1 = inputs.

| Bit | Port 0 (output, LED sink) | Port 1 (input) |
|---|---|---|
| 0 | SOS ACTIVE (red, `D4`) | LIGHT_DAY_n: LIGHTING at DAY = low |
| 1 | MASTER WARN (red, `D1`) | LIGHT_NIGHT_n: LIGHTING at NIGHT = low (both high = BLACKOUT) |
| 2 | MASTER CAUT (amber, `D2`) | PANEL_ID: strap `JP2`, open = variant A (high), closed = B (low) |
| 3 | CHARGING (white, `D10`) | SPARE1 (test point) |
| 4 | SAT (green, `D5`) | SPARE2 |
| 5 | MESH (green, `D6`) | SPARE3 |
| 6 | LTE (green, `D7`) | SPARE4 |
| 7 | GPS (green, `D8`) | SPARE5 |

`U2` at 0x23: port 0 = LED cathodes, port 1 = mixed.

| Bit | Port 0 (output, LED sink) | Port 1 |
|---|---|---|
| 0 | SHORE (green, `D9`) | TX_LAMPTEST (output): low lights the TX lamp through the BAT54 tie `D17` |
| 1 | MSG (white, `D11`) | SPARE6 |
| 2 | PI ring (amber, in the PI button) | SPARE7 |
| 3 | BAT1 (bar 20 %, amber, `D12`) | SPARE8 |
| 4 | BAT2 (40 %, green, `D13`) | SPARE9 |
| 5 | BAT3 (60 %, `D14`) | SPARE10 |
| 6 | BAT4 (80 %, `D15`) | SPARE11 |
| 7 | BAT5 (100 %, `D16`) | SPARE12 |

LED drive rule: an LED is ON when its port bit is configured as an OUTPUT driving 0; OFF when the bit is configured as an INPUT (high impedance). Never drive a 1: the anode rail is the 5 V LED rail and a 3.3 V push-pull high ghosts the LED. On boot the firmware first writes the configuration registers so every LED bit is an input, then the output registers to 0, then enables outputs one by one.

Hardware LEDs, no software: MAIN ring (alive whenever the LED rail is present), TX (red, `D3`, follows D8's real KEY line `TR_APRS` through `Q3`; the lamp-test tie lights it too).

## 5. Compute slots seen from the panel

The controller owns three lines per slot: `HBn` in, `SLOT_ENn` out, and the display switch selects. Rules:

- Boot: all three `SLOT_EN` high. A slot whose heartbeat stays flat for 60 s after its rail came up is shown as a slot fault (MASTER CAUT, the e-paper names the slot); the controller power-cycles it once (rail off 5 s) and then leaves it off until the operator acts.
- `HDMI_SEL` follows the slot the bridge names over USB; with no bridge instruction the controller selects the lowest slot with a live heartbeat.
- The PI button: short press = `PI_SHDN_REQ` (clean shutdown of every module), hold 8 s = `PI_KILL`. The MAIN PWR button is hardware to A22's power controller and the controller only sees its effect.

## 6. Hardware lines (work with the controller dead)

| Line | Source | Effect |
|---|---|---|
| TX_INHIBIT_n | `SW_EMCON` closed = low (100k pull-up `R14`, 10 nF `C24`) | D8's KEY gate cannot key the SA868 and the PA (`KEY = PTT_ANY AND TX_INHIBIT_n`); the bridge additionally holds every bearer |
| EMCON_HW | `U9` inverter, high while TX_INHIBIT_n is low | the transmitter rail gates on A22 and B16 (5G, LoRa, WiFi link, Zigbee, Thread, Iridium, HF, SDR) open; ruled 32.50 item 3 |
| ZEROIZE_HW | `SW_ZERO` closed = low | B16's secure element and the encrypted drives are wiped by the supervisor on the falling edge (hardware line to the slots); the controller shows the armed state |
| TR_APRS | D8's real KEY line | the TX lamp |
| MAIN PWR | `SW_MAIN` to A22 `J_MAINSW` | kit power |

## 7. The kit I2C bus (the controller is the master; the modules read everything over USB)

| Address | Device | Board |
|---|---|---|
| 0x10 | VEML7700 ambient light | C7 |
| 0x20, 0x25 | PCA9555 `U6` (outputs: switch reset, rail enables, module radio disables, 5G control) and `U7` (inputs: faults, RockBLOCK status) | B16 |
| 0x22, 0x23 | PCA9555 panel expanders (section 4) | C7 |
| 0x26 | PCA9555 (relay, PA and PTT status, mode inputs) | D8 |
| 0x40, 0x41, 0x44, 0x45, 0x46, 0x47 | INA226 rail monitors (node, PA, per-slot 5 V, outlet, wall port) | A22 |
| 0x49 | TMP117 board temperature under the coolers | B16 |
| 0x60 | ATECC608B secure element (keys behind ZEROIZE) | B16 |
| 0x68 | DS3231M holdover clock (CR2032 backed) | B16 |
| 0x6B | BQ25731 SMBus charger (the BB-2590/U pack) | A22 |

The sensor board inside E6 (BME688 at 0x76 and the IMU at 0x68 on its own bus) belongs to the sensor controller, a second RP2040 that is another USB device; its addresses never appear on the kit bus. Rules: the panel's 3.3 V is never switched while the ribbon is attached (an unpowered PCA9555 clamps the bus). With the ribbon unplugged the kit fails safe: A22 pulls `EMCON_HW` high (`R102`) and D8 pulls `TX_INHIBIT_n` low (`R2`), so every transmitter rail gate stays open and the PA cannot key; A22 pulls `ZEROIZE_HW` high (`R117`, no wipe) and `SHORE_INHIBIT` low (`R118`, charging allowed). A kit without its panel charges and computes but does not transmit.

## 8. Lighting

| LIGHTING position | Inputs | Controller action |
|---|---|---|
| DAY | LIGHT_DAY_n low | duty 100 %, the bridge sets the monitor's backlight full |
| NIGHT | LIGHT_NIGHT_n low | duty 15 %, backlight 20 %, sounder allowed |
| NVG | NIGHT plus the bridge's NVG mode | duty 2 % (the lowest PWM step the firmware allows), backlight 5 %, red and amber indicators only (16c) |
| BLACKOUT | both high, RAIL_SENSE low | duty 0, backlight off, the touch UI dark; the hardware rail is already open, software only follows |

One dimmer for the whole panel (MIL-STD-1472). The TX lamp is never dimmed below 10 % duty; in BLACKOUT it is dark like everything else (EMCON sits next to it for that case). The VEML7700 reading is reported to the bridge for automatic backlight control in DAY and NIGHT.

## 9. Indicator semantics, controls and the e-paper

Indicators as in the previous generation: MASTER WARN flashes (3 to 5 Hz) for any unacknowledged red condition (SOS active, ZEROIZE armed, thermal trip, pack under-voltage, slot fault), MASTER CAUT for an unacknowledged amber one (a bearer lost that was up, shore lost while charging was expected, disk nearly full, a slot fault), SAT / MESH / LTE / GPS steady for a bearer up (GPS: fix), SHORE and CHARGING from the charger over the bus, MSG for an unread inbound message, the PI ring as a 0.5 Hz heartbeat while any slot's bridge runs, BAT1..5 for 5 s after a lamp test or a short TEST press with the lowest LED flashing below 10 %. A flasher that fails must fail to steady-on (the controller holds the last written state when the USB link drops).

Controls: TEST / ACK short press = acknowledge (flashers to steady, sounder muted, MSG cleared, battery bar 5 s); hold 2 s = lamp test (all 17 LEDs 3 s, chirp, bar after); hold 5 s within 60 s of a "show QR" request from the touch UI = the e-paper shows the enrolment QR while held. SOS closed 2 s = SOS mode (flipping back cancels; the e-paper confirms both). EMCON = section 6 plus the software hold (queue every send, show EMCON on the e-paper, MASTER CAUT steady). ZEROIZE closed 5 s = the wipe (section 6); MASTER WARN flashes while armed; flipping back inside 5 s aborts; after the wipe the switch must be returned before the kit re-arms.

E-paper (PDi E2370KS0C1, 416 x 240, iTC controller, PDi's driving note rev 02): idle page with callsign, battery and shore state, GPS fix, UTC time, last inbound message, bearer chips and the slot health; refresh at most once a minute, full refresh once an hour and after ZEROIZE; the QR carries an enrolment URL plus a one-time code, never key material; the controller powers the display down through `EPD_PWR_n` between refreshes (the boost stage runs only while `BUSY` is high) and polls `BUSY` before every command.

Sounder patterns: chirp 50 ms (acknowledge), double chirp (lamp test), 1 s on 1 s off (SOS armed), continuous 3 s (ZEROIZE complete), muted in BLACKOUT.

## 10. Shore charge inhibit and the pack

`SHORE_INHIBIT` (controller GPIO 20, ribbon pin 26) reaches A22 through B16 and the dock strip E6 through A22's block: high = the shore and vehicle inputs are held off at the front end, so nothing charges. Low, floating or the controller dead = the inputs run, so a kit with a crashed panel still charges. The bridge asks the controller to assert it when the pack thermistor (read by the BQ25731 over the bus) is below 0 C, when the operator sets "no charge", and clears it with hysteresis (charge again above 3 C). Boot state: low. SHORE shows the charger's input-present bit, CHARGING its charge-in-progress bit; the pack is a BB-2590/U with its own gauge on the SMBus (the charger reads it), so the battery bar comes from the pack's state of charge.

## 11. Bridge protocol over USB (MESHSAT-837 defines the wire format)

The controller exposes a CDC serial port and a HID report. The bridge on the slot that owns the display sends: indicator states (one byte per LED), the lighting duty override, the sounder pattern, the e-paper page (a 1-bit bitmap or the idle page fields), the display-switch selection, the shutdown and kill commands, the charge inhibit. The controller reports: every switch state and edge with a timestamp, the heartbeat presence of the three slots, the light reading, the LED rail presence, the TX lamp state, the EMCON and ZEROIZE lines, its own firmware version. Any slot may read; only the display owner may write, and the owner is re-elected by the k3s cluster when its heartbeat stops (32.52).

# MeshSat control panel (PCB-C backer C7 under the face plate): software contract for the panel controller and the bridge

> Generation MESHSAT-830 (7 Sep 2026, `V2-SPEC.md`, appendix 32.52, 32.60). Nothing has been built: this contract describes the C7 backer as generated by `v2/ecad/tools/gen_sch_c.py` and is the specification for the panel controller firmware (MESHSAT-837) and for the bridge on the compute modules. The previous contract (the released C6 with the PCA9555 pair on the compute module's bus and the previous e-paper module on SPI0) is in the git history of this file.

Hardware record: `MESHSAT-709-geometry-appendix.md` sections 32.52 (fabric), 32.56 (face), 32.60 (C7 design). The panel is a **USB device**: an RP2040 panel controller (`U3`) on **bank 1's** USB hub over the `USB_PNL` pair of the 2x13 ribbon `J_PANEL`. Since the I/O high-availability work of 9 September 2026 a bank is not a module: bank 1's home host is the slot 1 module and its failover host is slot 2, selected in hardware (`ARCH-PCB-B-IOHA.md`), so the panel keeps a host when one module is lost. The bridge sees the panel move hosts, never disappear, unless two modules are gone. The compute modules never touch the panel's pins directly; every indicator, switch, the e-paper, the sounder and the light sensor are behind the controller, which presents one USB composite device (a CDC console for the bridge protocol and a vendor HID report for the state). The lines that act without any software are MAIN PWR, EMCON and the TX lamp (section 6). The ZEROIZE toggle is a hardware line too, but it reaches only the controller: the erase itself is the controller's firmware (decision 30, ruled by the owner on 26 September 2026, section 6).

> **Corrections of 26 September 2026 (MESHSAT-1357), read against the generators at `82dd1e4d`.** Nothing here has
> been built, so every correction is to what the generated boards WOULD do. (1) With the ribbon unplugged the
> kit does not "charge and compute but not transmit": `R102` on A22 pulls `EMCON_HW` LOW, not high, and the slot
> enables are pulled low, so no compute module runs and every EMCON gate is closed (section 7). (2) As generated
> the charger does not charge the 4S pack at all (its cell-count strap selects 2S) and, once that is fixed, it
> still needs a host, so a kit with a crashed panel does not usefully charge (section 10). (3) The pack's gauge
> is read by the sensor controller on E6, not by the charger, which has no conductor to the gauge and no
> thermistor input (sections 7 and 10). (4) EMCON removes the SUPPLY of most radios, so they stop receiving
> too, and it has no hardware path to the compute modules' own WiFi and Bluetooth (section 6); the owner ruled
> on 26 September that EMCON means the radios go dark (D-05), which makes the first a behaviour and the second a
> gap to close. (5) ZEROIZE is the owner's ruling of 26 September (decision 30): a crypto-erase through the
> secure element, performed by this controller (sections 1, 5, 6 and 9). (6) `PI_SHDN_REQ` is active low
> (section 3). (7) The boot order is written out, because the expanders power up with every output high
> (section 5). (8) SOS has a defined action for the first time, the owner's ruling of 26 September (D-10,
> section 9).

> **Corrections of 26 September 2026, second pass (MESHSAT-1357, open item S-07), read against the generators and
> the committed netlists at `45bde541`**, which carry the circuit corrections of `faf8c981`, `458b2873` and `d90f30e4`.
> Nothing has been built, so each is again a correction to what the generated boards WOULD do. (9) Items (2) and (4)
> above describe circuits since replaced: the charger's strap selects 4S and the kit's loads sit on the charger's
> system side, so shore carries them without software and the host-free trickle goes to the pack (section 10);
> EMCON removes the WiFi link cards' supplies and reaches the compute modules' own WiFi and Bluetooth through open
> drains, and what stays open is the 5G module and the line's shared items (section 6). (10) The EMCON buffer `U9` is
> a 74LVC1G17 with a Schmitt-trigger input (section 1). (11) The ZEROIZE toggle is sensed on this board only, on the
> local net `ZEROIZE_SW`, and `U12` copies it onto `ZEROIZE_HW` (sections 1, 3, 5 and 6). (12) The secure element's
> slot map is worked out from Microchip's public documents in `feasibility/ZEROIZE.md`: the documentary half of the
> precondition is closed and the bench half is open (section 6). (13) The kit bus carries targets section 7 did not
> list: board A's two expanders, the three I/O supervisors, the Ethernet switch and the PoE controller, and the PoE
> controller answers a broadcast address that is also a supervisor's (section 7, an open finding).

## 1. What sits on the backer

| Function | Parts (schematic references) | Owner |
|---|---|---|
| Panel controller | `U3` RP2040, `U4` W25Q16 QSPI flash, `Y1` 12 MHz, `R5`/`JP1` BOOTSEL strap, `TP1`..`TP3` SWD | firmware MESHSAT-837 |
| Indicators | 16 THT LEDs `D1`..`D16` through Mentor light guides in the plate, sinks on two PCA9555 (`U1` 0x22, `U2` 0x23), the TX lamp `D3` on `Q3` | controller |
| LED rail | `+5V` through the LIGHTING toggle `SW_LIGHT` (pole 1) to `LED_RAIL_SW`, `Q1` P-FET high side switched by `Q2` from `PANEL_PWM`, `RAIL_SENSE` divider | controller |
| Switches | `SW_MAIN` (19 mm, hardware lead to A22 `J_MAINSW`), `SW_PI` (16 mm, lead pads `PIJ2_A/B`), `SW_TEST` (16 mm, `TEST_SW`), `SW_SOS`, `SW_EMCON`, `SW_ZERO` (APEM locking toggles with hinged covers), `SW_LIGHT` (NKK DPDT ON-ON-ON) | mixed, section 4 |
| E-paper | Pervasive Displays E2370KS0C1 on the 24-way ZIF `J_EPD`, supply switch `Q5`, the boost stage `L1`/`Q6`/`D19`..`D21` per PDi's driving note rev 02 | controller |
| Sounder | `BZ1` IP67 panel sounder on `+5V` through `Q4` from `PWM1` | controller |
| Ambient light | `U_LIGHT` VEML7700 (I2C 0x10) under its own light guide | controller |
| ZEROIZE | `SW_ZERO` pulls the local net `ZEROIZE_SW` low (10k pull-up `R10`, 10 nF `C20`), which the controller reads on GPIO 22; `U12`, a 74LVC1G17 buffer, copies it onto `ZEROIZE_HW` for the ribbons, where the line reaches a test point on B16, a 10k pull-up (`R117`) on A22 and a test point on D8, and no other part listens (decision 30; `gen_sch_c.py:168-178` and `:217` at `45bde541`, corrected 26 September 2026: until `faf8c981` the toggle sat on `ZEROIZE_HW` itself) | controller firmware: the crypto-erase of section 6 |
| EMCON logic | `U9` 74LVC1G17 Schmitt-trigger buffer: `EMCON_HW` follows `TX_INHIBIT_n`, both LOW = inhibited (corrected 9 September 2026, red team C1: the part was an inverter and both consumer boards were built on low = silence, so asserting EMCON enabled the PA and released both M.2 radios; a Schmitt input since `faf8c981`, because the toggle's node rises with a 77 us time constant, far outside the 74LVC1G34's input slew limit, `gen_sch_c.py:157-166`). No part on this board shows the line's state without firmware: the hardware EMCON lamp of SD-EMC-6 is owed (S-44) | hardware |
| Pass-throughs | the Xenarc 709GNK monitor's connector block (its HDMI, USB touch and 12 V leads go below the plate to B16 and A22), two U-174/U headset jacks `J_HSJ1`/`J_HSJ2` (leads to D8 `J_HS1`/`J_HS2`), the USB camera module behind the sealed window (lead to B16 `J_CAM`) | not the panel's |

## 2. The ribbon `J_PANEL` (2x13, 2.54 mm; B16 `J_PANEL` carries the same map, checked by `check_contracts.py`)

| Pin | Net | Direction (seen from the panel) | Pin | Net | Direction |
|---|---|---|---|---|---|
| 1 | +5V | in (rail) | 14 | GND | |
| 2 | +5V | in | 15 | USB_PNL_P | USB 2.0 device pair to B16's bank 1 hub (home host slot 1, failover slot 2) |
| 3 | GND | | 16 | USB_PNL_N | |
| 4 | SDA | the kit I2C bus, the controller is its master | 17 | GND | |
| 5 | SCL | | 18 | HB1 | in: slot 1 heartbeat |
| 6 | EXP_INT | out (the panel expanders' interrupt, also read by the controller) | 19 | HB2 | in: slot 2 heartbeat |
| 7 | TR_APRS | in: D8's real KEY line (lights the TX lamp) | 20 | HB3 | in: slot 3 heartbeat |
| 8 | EMCON_HW | out, hardware: LOW = transmitters inhibited | 21 | SLOT_EN1 | out: slot 1 rail enable |
| 9 | GND | | 22 | SLOT_EN2 | out |
| 10 | ZEROIZE_HW | out, hardware: `U12`'s copy of the ZEROIZE toggle, low = closed; no part on another board acts on it (section 6) | 23 | SLOT_EN3 | out |
| 11 | TX_INHIBIT_n | out, hardware: low = inhibit (to D8's KEY gate) | 24 | PI_SHDN_REQ | out: clean shutdown request to the modules |
| 12 | HDMI_SEL1 | out: display switch input select | 25 | PI_KILL | out: hard power off request to A22 |
| 13 | HDMI_SEL2 | out | 26 | SHORE_INHIBIT | out: to A22 and the dock (charge inhibit) |

## 3. Controller pin map (RP2040 `U3`)

| GPIO | Net | Use |
|---|---|---|
| 0, 1 | SDA, SCL | the kit I2C bus (section 7), the controller is the master |
| 2, 3 | EPD_SCL, EPD_SDA | e-paper SPI (the iTC's 4-wire mode; MOSI only, the display's read path is unused) |
| 4, 5, 6, 7 | EPD_DC, EPD_CS, EPD_RST, EPD_BUSY | e-paper control; BUSY is high while the display refreshes |
| 8 | PANEL_PWM | LED rail dimmer through `Q2`/`Q1`, 2 kHz, 10 bit; boots low (rail dark until the firmware is up) |
| 9 | PWM1 | sounder through `Q4`; high = sound, PWM for patterns |
| 10, 11, 12 | HB1, HB2, HB3 | heartbeat inputs, one per compute slot (a slot toggles its line at 1 Hz while its supervisor runs) |
| 13, 14, 15 | SLOT_EN1..3 | slot rail enables (high = on), section 5 |
| 16, 17 | HDMI_SEL1, HDMI_SEL2 | the three-input display switch on B16: which slot drives the Xenarc |
| 18 | PI_SHDN_REQ | shutdown request to every module, ACTIVE LOW: the net is shared with A22's LTC2954 open-drain INT (pulled up by `R3`), so the controller pulls it low for at least 200 ms and otherwise leaves it released; it never drives it high, and it reads the pin as an input to see MAIN taps |
| 19 | PI_KILL | hard kill to A22's power controller (held high 3 s) |
| 20 | SHORE_INHIBIT | charge inhibit to A22 and the dock, section 9 |
| 21 | EMCON_HW | read back of the hardware EMCON line |
| 22 | ZEROIZE_SW | the ZEROIZE toggle, local to this board (10k pull-up, 10 nF); `U12` copies it onto `ZEROIZE_HW` for the other boards (since `faf8c981`) |
| 23 | TR_APRS | read back of D8's KEY line (the TX lamp state) |
| 24 | EXP_INT | the expanders' interrupt, active low |
| 25 | LED_STAT | the controller's own status LED `D18` (on the board, not on the face) |
| 26 (ADC0) | RAIL_SENSE | `LED_RAIL_SW` through 10k: the LED rail is present when the LIGHTING toggle is not at BLACKOUT |
| 27 | TEST_SW | TEST / ACK button, pressed = low (10k pull-up, 10 nF) |
| 28 | SOS_SW | SOS locking toggle, closed = low (10k pull-up, 10 nF) |
| 29 | EPD_PWR_n | the e-paper's 3.3 V switch `Q5`, low = on |

Debounce 30 ms on every switch input; the two toggles are maintained switches (pull, flip, leave).

## 4. Expanders (PCA9555, on the controller's I2C bus, INT wire-ORed on `EXP_INT`)

`U1` at 0x22: port 0 = LED cathodes (sinks), port 1 = inputs.

| Bit | Port 0 (output, LED sink) | Port 1 (input) |
|---|---|---|
| 0 | SOS ACTIVE (red, `D4`) | LIGHT_DAY_n: LIGHTING at DAY = low |
| 1 | MASTER WARN (red, `D1`) | LIGHT_NIGHT_n: LIGHTING at NIGHT = low (both high = BLACKOUT) |
| 2 | MASTER CAUT (amber, `D2`) | PANEL_ID: strap `JP2`, open = variant A (high), closed = B (low) |
| 3 | CHARGING (white, `D10`) | SPARE1 (test point) |
| 4 | SAT (green, `D5`) | SPARE2 |
| 5 | MESH (green, `D6`) | SPARE3 |
| 6 | LTE (green, `D7`) | SPARE4 |
| 7 | GPS (green, `D8`) | SPARE5 |

`U2` at 0x23: port 0 = LED cathodes, port 1 = mixed.

| Bit | Port 0 (output, LED sink) | Port 1 |
|---|---|---|
| 0 | SHORE (green, `D9`) | TX_LAMPTEST (output): low lights the TX lamp through the BAT54 tie `D17` |
| 1 | MSG (white, `D11`) | SPARE6 |
| 2 | PI ring (amber, in the PI button) | SPARE7 |
| 3 | BAT1 (bar 20 %, amber, `D12`) | SPARE8 |
| 4 | BAT2 (40 %, green, `D13`) | SPARE9 |
| 5 | BAT3 (60 %, `D14`) | SPARE10 |
| 6 | BAT4 (80 %, `D15`) | SPARE11 |
| 7 | BAT5 (100 %, `D16`) | SPARE12 |

LED drive rule: an LED is ON when its port bit is configured as an OUTPUT driving 0; OFF when the bit is configured as an INPUT (high impedance). Never drive a 1: the anode rail is the 5 V LED rail and a 3.3 V push-pull high ghosts the LED. On boot the firmware first writes the configuration registers so every LED bit is an input, then the output registers to 0, then enables outputs one by one.

Hardware LEDs, no software: MAIN ring (alive whenever the LED rail is present), TX (red, `D3`, follows D8's real KEY line `TR_APRS` through `Q3`; the lamp-test tie lights it too).

## 5. Compute slots seen from the panel

The controller owns three lines per slot: `HBn` in, `SLOT_ENn` out, and the display switch selects. Rules:

- Boot, in this order: `PI_KILL` driven low (push-pull, and never released while any slot is powered: on the netlist a powered slot's level stage can lift it and switch the kit off, a mechanism not yet measured); `PI_SHDN_REQ` released; the ZEROIZE toggle read (`ZEROIZE_SW`, GPIO 22), and if it is closed, or the flash holds a wipe-pending record, the erase of section 6 is completed before any slot powers; every expander's output register written before its configuration register (the default output register is all ones, so configuration first would switch on the PoE and USB-C stages), keeping `DEV_EN` at 1, which is written 0 only as the last act of a shutdown; the charger's registers written; then the three `SLOT_EN` high, one at a time. A slot whose heartbeat stays flat for 60 s after its rail came up is shown as a slot fault (MASTER CAUT, the e-paper names the slot); the controller power-cycles it once (rail off 5 s) and then leaves it off until the operator acts.
- `HDMI_SEL` follows the slot the bridge names over USB; with no bridge instruction the controller selects the lowest slot with a live heartbeat.
- The PI button: short press = `PI_SHDN_REQ` (clean shutdown of every module), hold 8 s = `PI_KILL`. The MAIN PWR button is hardware to A22's power controller and the controller only sees its effect.

## 6. Hardware lines (work with the controller dead)

| Line | Source | Effect |
|---|---|---|
| TX_INHIBIT_n | `SW_EMCON` closed = low (10k pull-up `R14` on C7, 10 nF `C24`; A22, B16 and D8 each hold it down with 100k so a cut ribbon inhibits) | D8's KEY gate cannot key the SA868 and the PA (`KEY = PTT_ANY AND TX_INHIBIT_n`); the bridge additionally holds every bearer |
| EMCON_HW | `U9` buffer, low while TX_INHIBIT_n is low; with the ribbon out B16's `R58` (10k) and A22's `R102` (100k) hold it low | as generated at `45bde541`: on A22 it is ANDed into the enables of the PA and HF converters (`U26`, `gen_sch_a.py:1102`); on B16 it is ANDed into the supply enables of the LimeSDR, the RockBLOCK, the LoRa module and both E72 (`U19`, `U20`, `gen_sch_b.py:974-977`), and, inverted once into `EMCON_ON` (`Q11`, `R513`, `:1029`), it removes the supply of the two WiFi link cards (`Q111` and `Q311` on their bucks' enables, `:478`), pulls `W_DISABLE1#` low on all three M.2 cards through open drains (`Q106`, `Q206`, `Q306`, `:621`, `:644`, `:713`) and pulls the three compute modules' `WL_nDisable` and `BT_nDisable` low through open drains that the slot's `U{s}11` drives (kill = the `U6` request OR `EMCON_ON`, `:716-741`); the three supervisors read it on PC5; D8's KEY gate makes VHF/APRS the one transmit-only case. Ruled 32.50 item 3 ("every transmitter") and, on 26 September 2026, by the owner's D-05: **under EMCON the radios go DARK.** Every radio with an emission path is powered off or RF-disabled in hardware; the VHF path keeps listening (D8's gate is on transmit only, as ruled); GNSS, DCF77 and the lightning detector continue. What is still open is session engineering under that ruling (S-01): the 5G module's only path is `W_DISABLE1#`, which Quectel documents as a firmware-mediated airplane mode, until the staged removal of its supply (SD-EMC-1) is drawn on B16; the line's shared items (`feasibility/EMCON.md` section 7; among them, a loss of B16's `+3V3_DEV` releases every gate that hangs on `EMCON_ON` or on `U{s}11`); and the back-feed paths of SD-EMC-2. No row has been shown on a bench (`feasibility/EMCON.md` section 6). **Corrected 26 September 2026 (S-07):** this row said the compute modules' own radios had no hardware path from EMCON and the WiFi cards only a disable pin of unproven effect, which was the circuit before `458b2873` |
| ZEROIZE_HW | `SW_ZERO` closed = `ZEROIZE_SW` low, and `U12` drives `ZEROIZE_HW` low with it | the toggle is read by the controller's GPIO 22 on `ZEROIZE_SW`, the only listener: `ZEROIZE_HW` reaches test points on B16 and D8 and A22's pull-up `R117`, and no hardware line reaches the slots, the supervisors or the drives. Decision 30, ruled by the owner on 26 September 2026: a CRYPTO-ERASE through the secure element. Every drive and eMMC key is wrapped by key-encryption keys only B16's ATECC608B holds (two of them, both needed to unlock a drive, `feasibility/ZEROIZE.md` section 3); held closed 5 s, the controller destroys both first over the kit bus by regenerating them in place (the GenKey command), then tells the running modules to drop the keys they hold in RAM, then drops `SLOT_EN1..3`. It is firmware and provisioning, and it rests on one precondition: that the fitted part lets those keys be destroyed after its zones are locked. `feasibility/ZEROIZE.md` closes that from Microchip's public documents (a desk result; the full ATECC608B datasheet stays under NDA) and leaves the physical demonstration on the fitted part to bench experiments Z-EXP-A and Z-EXP-B (S-35), with the SLB 9673 TPM 2.0 on B16's `U8` site as the fallback. Accepted by the owner: a drive unlocks at boot only through the secure element, this controller and the kit bus. The controller shows the armed state |
| TR_APRS | D8's real KEY line | the TX lamp |
| MAIN PWR | `SW_MAIN` to A22 `J_MAINSW` | kit power |

## 7. The kit I2C bus (the controller is the master; the modules read everything over USB)

| Address | Device | Board |
|---|---|---|
| 0x10 | VEML7700 ambient light | C7 |
| 0x20, 0x25 | PCA9555 `U6` (outputs: switch reset, rail enables, the module radio off requests into the EMCON gates `U{s}11`, 5G control) and `U7` (inputs: faults, RockBLOCK status) | B16 |
| 0x21, 0x24 | PCA9555 `U27` (outputs: the charge inhibit, the monitor, heater, D8 and device-rail enables, the PoE, PA and HF software enables; inputs: faults and status) and `U28` (power-good lines, the USB-C outlet's software enable, the Glenair host port's VBUS switch and its fault, spares) | A22 |
| 0x22, 0x23 | PCA9555 panel expanders (section 4) | C7 |
| 0x26 | PCA9555 (relay, PA and PTT status, mode inputs) | D8 |
| 0x28 | TPS23861 PoE PSE controller `U5`, at its factory address with its A3 pin open (pulled up inside the part); every TPS23861 also answers the broadcast address 0x30 (TI SLUSBX9I, sections 7.3.13 and 8.3.3.1) | B16 |
| 0x30, 0x31, 0x32 | the three STM32H743 I/O supervisors `U41`, `U51`, `U61`, I2C1 targets on PB6 and PB7 for status only (`ARCH-PCB-B-IOHA.md` section 6; the addresses are the supervisors' firmware) | B16 |
| 0x40, 0x41, 0x44, 0x45, 0x46, 0x47 | INA226 rail monitors: the three slot rails (5.1 V), the device rail, the PA rail (13.8 V), the PoE rail (54 V) | A22 |
| 0x49 | TMP117 board temperature under the coolers | B16 |
| 0x5F | KSZ9897R Ethernet switch, I2C management (a fixed address: "The 7-bit device address is fixed as 1011_111", Microchip's datasheet in `v2/vendor/microchip/`; I2C management selected by the strap `R57`) | B16 |
| 0x60 | ATECC608B secure element (keys behind ZEROIZE); 0x60 is the family's default, and the fitted SSHDA part's factory address is item U4 of `feasibility/ZEROIZE.md`, TBD until the bench | B16 |
| 0x68 | DS3231M holdover clock (CR2032 backed) | B16 |
| 0x6B | BQ25731 charger (an I2C target on the kit bus; it has no conductor to the pack's gauge and no thermistor input) | A22 |

Read from the SDA and SCL nets of the committed netlists of boards A, B, C and D at `45bde541`. **Corrected 26 September 2026 (S-07):** the table listed neither board A's two expanders nor the three supervisors, the Ethernet switch and the PoE controller. **An open finding of that correction:** 0x30 is both supervisor 1's status address and the TPS23861's broadcast address, which every TPS23861 answers whatever its A3 pin says, so a transaction the controller means for the supervisor reaches the PoE controller too. The broadcast address is fixed in the TPS23861, so it is supervisor 1's address that has to move, in the supervisors' firmware; nothing is resolved here. Three firmware-bearing supervisors share this bus with the secure element; only the panel controller is to command it (firmware rule Z-C3 of `feasibility/ZEROIZE.md`, residual R7).

The sensor board inside E6 (BME688 at 0x76 and the IMU at 0x68 on its own bus) belongs to the sensor controller, a second RP2040 that is another USB device; its addresses never appear on the kit bus. Rules: the panel's 3.3 V is never switched while the ribbon is attached (an unpowered PCA9555 clamps the bus). With the ribbon unplugged the kit fails safe on transmit: A22 pulls `EMCON_HW` low (`R102`, as B16 does with `R58`) and A22, B16 and D8 pull `TX_INHIBIT_n` low (`R145`, `R59`, `R2`), so every EMCON gate closes and the PA cannot key; A22 pulls `ZEROIZE_HW` high (`R117`, no wipe) and `SHORE_INHIBIT` low (`R118`, the inputs run). The slot enables are pulled low on A22, so no compute module runs. A kit without its panel neither computes nor transmits, and its charger has no host: at best the pack gains the host-free 256 mA less board E's always-on draw (section 10).

## 8. Lighting

| LIGHTING position | Inputs | Controller action |
|---|---|---|
| DAY | LIGHT_DAY_n low | duty 100 %, the bridge sets the monitor's backlight full |
| NIGHT | LIGHT_NIGHT_n low | duty 15 %, backlight 20 %, sounder allowed |
| NVG | NIGHT plus the bridge's NVG mode | duty 2 % (the lowest PWM step the firmware allows), backlight 5 %, red and amber indicators only (16c) |
| BLACKOUT | both high, RAIL_SENSE low | duty 0, backlight off, the touch UI dark; the hardware rail is already open, software only follows |

One dimmer for the whole panel (MIL-STD-1472). The TX lamp is never dimmed below 10 % duty; in BLACKOUT it is dark like everything else (EMCON sits next to it for that case). The VEML7700 reading is reported to the bridge for automatic backlight control in DAY and NIGHT.

## 9. Indicator semantics, controls and the e-paper

Indicators as in the previous generation: MASTER WARN flashes (3 to 5 Hz) for any unacknowledged red condition (SOS active, ZEROIZE armed, thermal trip, pack under-voltage, slot fault), MASTER CAUT for an unacknowledged amber one (a bearer lost that was up, shore lost while charging was expected, disk nearly full, a slot fault), SAT / MESH / LTE / GPS steady for a bearer up (GPS: fix), SHORE and CHARGING from the charger over the bus, MSG for an unread inbound message, the PI ring as a 0.5 Hz heartbeat while any slot's bridge runs, BAT1..5 for 5 s after a lamp test or a short TEST press with the lowest LED flashing below 10 %. A flasher that fails must fail to steady-on (the controller holds the last written state when the USB link drops).

Controls: TEST / ACK short press = acknowledge (flashers to steady, sounder muted, MSG cleared, battery bar 5 s); hold 2 s = lamp test (all 17 LEDs 3 s, chirp, bar after); hold 5 s within 60 s of a "show QR" request from the touch UI = the e-paper shows the enrolment QR while held. SOS closed 2 s = SOS mode (flipping back cancels; the e-paper confirms both). **What SOS does (D-10, owner ruling of 26 September 2026; firmware only, no board change):** the bridge sends a distress message with the kit's position over the bearers that are up, Iridium first when nothing else is, to a configured recipient list. It never transmits through EMCON: with EMCON asserted the message is queued until EMCON is released and the operator is told on the panel (how, exactly, is the session's to define with the rest of the indications). SOS is part of prototype 1's core acceptance, taken by the session under the owner's standing rule of 26 September 2026, because D-10 defines its action and the foundation decision table recommended it. EMCON = section 6 plus the software hold (queue every send, show EMCON on the e-paper, MASTER CAUT steady). ZEROIZE closed 5 s = the erase of section 6, and it is the ONLY trigger (owner ruling of 26 September 2026): the tamper or lid switch is logged and never wipes, and there is no remote wipe in this revision. MASTER WARN flashes while armed; flipping back inside 5 s aborts. The line is LEVEL-SENSITIVE across a power loss: at boot the controller reads it before any slot powers, a toggle still closed or a wipe-pending record in the controller's flash completes the erase, and the kit re-arms only when the switch is returned.

E-paper (PDi E2370KS0C1, 416 x 240, iTC controller, PDi's driving note rev 02): idle page with callsign, battery and shore state, GPS fix, UTC time, last inbound message, bearer chips and the slot health; refresh at most once a minute, full refresh once an hour and after ZEROIZE; the QR carries an enrolment URL plus a one-time code, never key material; the controller powers the display down through `EPD_PWR_n` between refreshes (the boost stage runs only while `BUSY` is high) and polls `BUSY` before every command.

Sounder patterns: chirp 50 ms (acknowledge), double chirp (lamp test), 1 s on 1 s off (SOS armed), continuous 3 s (ZEROIZE complete), muted in BLACKOUT.

## 10. Shore charge inhibit and the pack

`SHORE_INHIBIT` (controller GPIO 20, ribbon pin 26) reaches A22 through B16 and the dock strip E6 through A22's block: high = the shore and vehicle inputs are held off at the front end, so nothing charges. Low, floating or the controller dead = the inputs run. As generated since `458b2873` the charger's cell-count strap is `R26` 13.3k over `R27` 40.2k, 75.1 % of VDDA, inside the BQ25731's 4S window of 68.4 to 81.5 % (ChargeVoltage 16.8 V, SYSOVP and VSYS_MIN at their 4S defaults; TI SLUSE66A 8.5 and Table 9-2; `gen_sch_a.py:764-771` at `45bde541`), and the kit's loads sit on the charger's system node VSYS (the net `VBAT`), with the pack beyond the charge shunt `R17` and the 25 A blade `F1`, TI's own topology (SLUSE66A Figure 10-1; `gen_sch_a.py:18-48`). So shore carries the loads up to the charger's input limit without any software, about 1.6 to 1.7 A at the 20 V bus (about 31 W) until the host writes IIN_HOST, and the pack supplies above it. The charger still needs a host (the kit-bus master writes its registers and services its 175 s watchdog; it never terminates a charge by itself, SLUSE66A 9.4.1): ChargeCurrent() is 256 mA at power-on by TI's answer (E2E thread 1316778, 23 January 2024) against 0 A in the datasheet's prose, it returns to 256 mA whenever the watchdog expires without a host write (SLUSE66A 9.3.21.1, the `WDTMR_ADJ` field), and whether the charger charges before any host write is a bench question (TBD). That trickle goes to the pack alone, less board E's always-on draw, which sits on the pack side: a kit with a crashed panel is carried by shore at idle and its pack is held rather than charged (`review-packets/battery/CHARGER-STATE-SEQUENCE.md` sections 2 and 4 to 6). **Corrected 26 September 2026 (S-07):** this paragraph said the strap selected 2S (`R26` 60.4k) and that the 256 mA fed the loads and the pack together, which was the circuit before `458b2873`. The bridge asks the controller to assert it when the pack temperature (the pack gauge's own thermistor, read by the sensor controller on E6 and reported over USB) is below 0 C, when the operator sets "no charge", and clears it with hysteresis (charge again above 3 C). Boot state: low. SHORE shows the charger's input-present bit, CHARGING its charge-in-progress bit; the pack is a built 4S smart pack with its own SMBus gauge, read by the sensor controller on E6 (not by the charger), so the battery bar comes from the pack's state of charge as the sensor controller reports it. As generated since `faf8c981` that path exists: both ends of the pack's SMBus lead are the same JST-XH 1x4 in the pack board's order (1 SMBC, 2 SMBD, 3 the pack's own negative lead, 4 PRES), so a straight four-way lead mates them, and `check_contracts.py` checks the lead as a cross-board contract (ASSEMBLY.md section 4). **Corrected 26 September 2026:** this paragraph said E6's end was a 2.54 mm 1x6 header that no lead mated, which was true before `faf8c981`.

## 11. Bridge protocol over USB (MESHSAT-837 defines the wire format)

The controller exposes a CDC serial port and a HID report. The bridge on the slot that owns the display sends: indicator states (one byte per LED), the lighting duty override, the sounder pattern, the e-paper page (a 1-bit bitmap or the idle page fields), the display-switch selection, the shutdown and kill commands, the charge inhibit. The controller reports: every switch state and edge with a timestamp, the heartbeat presence of the three slots, the light reading, the LED rail presence, the TX lamp state, the EMCON and ZEROIZE lines, its own firmware version. Any slot may read; only the display owner may write, and the owner is re-elected by the k3s cluster when its heartbeat stops (32.52).

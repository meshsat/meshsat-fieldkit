# PHASE B: where each rule of pcb_rules.yaml is generated, where it is verified, and what its evidence is
# worth (MESHSAT-862, 16 September 2026). Written AFTER the registry, by reading the tools; the registry was
# written first so that the catalogue is not a description of the gates that happen to exist.
#
# maturity meanings (rules_lib.MATURITY), applied strictly:
#   ENFORCED           an executable decision gate exists, writes a machine-readable verdict, and has
#                      behavioural fixtures (a failing input that must fail, a passing input that must pass)
#   GENERATED_ONLY     generation intends to comply; no independent verification of the property
#   VERIFIED_MANUALLY  a controlled review, calculation, simulation or lab protocol exists with its artefact
#   DOCUMENTED_ONLY    prose exists, no current evidence
#   OPEN               no adequate implementation or verification
#   SOURCE_UNVERIFIED  the governing authority or threshold is not established
#   OWNER_DECISION_REQUIRED  more than one valid choice and the reserved decision is unresolved
#
# gap_category is one of the nine the audit separates:
#   ABSENT GENERATED_ONLY PROSE_ONLY NO_BEHAVIOURAL_TEST NO_PROTOCOL DUPLICATED_OR_STALE HEURISTIC_AS_LAW
#   SOURCE_OR_APPLICABILITY_UNRESOLVED CROSS_BOARD_INCONSISTENT   (or NONE when the rule is covered)

schema_version: "1.0.0"
generated: "2026-09-16"
rule_set_fingerprint: "88f207606ad2945a"

coverage:

 ENV-001: {implementation: NONE_YET, verification: {tool: none}, maturity: OPEN, gap_category: ABSENT,
   note: "no envelope document exists: temperature, humidity, altitude, vibration and single-fault assumptions are unwritten, and every derating rule resolves against nothing",
   remediation: {action: "write the envelope from the owner's use case and the enclosure's own ratings", owner: OWNER, depends_on: [], p50_h: 3, p80_h: 8}}

 ENV-002: {implementation: "claims_check.py plus claims-allow.txt, one declared exemption per line with its reason",
   verification: {tool: claims_check.py, verdict: claims_check, fixtures: tests/test_gate_fixtures.py},
   maturity: ENFORCED, gap_category: NONE,
   note: "16 September 2026: a screen over the eight public documents, in final_gate.py because the release package is a set of
     documents as much as a set of folders and they reach the public mirror within minutes. It finds the sentences that assert a
     rating or a capability and asks each for a qualifier, an evidence reference, a negation, or a declared reason. The
     distinction it holds: a rating attached to a part the project BUYS is the maker's own claim and quoting it is a fact about
     the bill of materials, while the same rating attached to THIS KIT is a promise about hardware that has never been built.
     Nine vendor ratings are declared with their reason; 19 claim sentences, 0 unqualified"}

 SCH-001: {implementation: "gen_sch_*.py", verification: {tool: erc_gate.py, verdict: erc_gate, fixtures: tests/test_erc_gate.py},
   maturity: ENFORCED, gap_category: NONE, note: "an allow line without a reason waves nothing through, proved by fixture"}

 SCH-002: {implementation: "gen_pcb_*3.py", verification: {tool: netlist_board.py, verdict: netlist_board, fixtures: tests/test_netlist_board.py},
   maturity: ENFORCED, gap_category: NONE, note: "three comparisons with denominators; runs on the placed and on the routed board"}

 SCH-003: {implementation: "gen_sch_*.py", verification: {tool: check_contracts.py, verdict: check_contracts, fixtures: tests/test_gate_fixtures.py},
   maturity: ENFORCED, gap_category: NONE,
   note: "INCONCLUSIVE when a sibling netlist is absent, which is correct; the contracts themselves are a hand-written list and their completeness is not itself checked"}

 SCH-004: {implementation: "gen_sch_b.py, gen_sch_c.py, gen_sch_d.py",
   verification: {tool: check_contracts.py, verdict: check_contracts, document: "docs/OWNER-DECISIONS-2026-09-11.md"},
   maturity: OWNER_DECISION_REQUIRED, gap_category: ABSENT,
   note: "the transmit-inhibit chain is checked and passes. ZEROIZE is not: the panel toggle drives a line that reaches
     the connector and a test point on boards A and B and NOTHING ELSE, the ATECC608B carries only power and I2C, and
     the feature as built asks the compute modules to wipe themselves in software. 16 September 2026: written up as
     owner decision 30 with three costed options. The fact that shapes them is that the ATECC608B has NO ERASE PIN, so
     no option is a pure hardware wipe; the fact that makes option 1 cheap is that the three supervisors are already on
     the same I2C bus as the secure element and depend on no compute module being alive",
   remediation: {action: "owner decision 30: the supervisors execute the wipe, power removal only, or accept the software path and rename the feature everywhere",
     owner: OWNER, depends_on: [], p50_h: 8, p80_h: 24}}

 CMP-001: {implementation: "gen_sch_*.py value strings and each board's intent rails",
   verification: {tool: derate.py, verdict: derate, fixtures: tests/test_gate_fixtures.py},
   maturity: GENERATED_ONLY, gap_category: NO_BEHAVIOURAL_TEST,
   note: "16 September 2026: the VOLTAGE half is implemented and gated. Every part whose value string carries a rating is
     compared with the declared voltage of every rail its pins touch, at a 20 percent margin, and board A's 48 rated
     pairs come back clean, which is owner ruling 10's fix confirmed rather than re-found. THREE THINGS ARE STILL OPEN
     and the tool says so in its own output rather than implying coverage it does not have: a part whose value carries no
     rating (90 of them on board A) has its absolute maximum in a datasheet nothing reads; DC-BIAS CAPACITANCE DERATING
     is not implemented at all, and it is a different failure (a decoupling network that is not there, rather than a part
     failing short); and a rated part on a net with no declared voltage is reported as UNDECLARED, 29 nets on board A,
     mostly switch and bootstrap nodes. That is why this is GENERATED_ONLY and not ENFORCED",
   remediation: {action: "declare the switching and bootstrap nodes voltages so their parts can be judged; then the DC-bias curves for the decoupling on the fastest rails",
     owner: SESSION, depends_on: [PWR-001], p50_h: 6, p80_h: 18}}

 CMP-002: {implementation: "lcsc_fill.py", verification: {tool: "jlc_certify.py, lcsc_fill.py", verdict: "jlc_certify, lcsc_fill", fixtures: "tests/test_order_codes.py, tests/test_gate_fixtures.py"},
   maturity: ENFORCED, gap_category: NONE, note: "package mismatch is caught against the supplier's own record, with a date"}

 SUP-001: {implementation: "lcsc_fill.py, jlc-handfit.txt",
   verification: {tool: "jlc_certify.py, lcsc_fill.py", verdict: "jlc_certify, lcsc_fill", fixtures: "tests/test_order_codes.py, tests/test_gate_fixtures.py"},
   maturity: ENFORCED, gap_category: NONE, note: "stock is read at a date; the date is the evidence's freshness"}

 PWR-001: {implementation: "gen_sch_*.py via intent.rail()", verification: {tool: intent_checks.py, verdict: intent_rails, fixtures: tests/test_rail_loads.py},
   maturity: ENFORCED, gap_category: NONE,
   note: "16 September 2026: the declared set is compared with the power nets the board actually carries. A net whose
     name begins with + is a rail by this project's own generator convention, so intent_checks counts the power-symbol
     nets against the declared ones and names what is missing. It found thirty-eight undeclared rails across five
     boards, thirty of them on board B, and every one of those was invisible three ways at once: judged as a SIGNAL by
     the return-path rule, skipped entirely by dc_drop, and unreachable by derate. All thirty-eight are declared with
     their loads and their real power-path sources"}

 PWR-002: {implementation: "gen_sch_a.py, gen_sch_b.py", verification: {tool: none}, maturity: OPEN, gap_category: ABSENT,
   note: "sequencing exists in the design (LTC2954, enables, eFuses) and is written nowhere as a requirement with a check",
   remediation: {action: "a sequencing sheet per board from the enables in the netlist, reviewed against each module's requirement", owner: SESSION, depends_on: [], p50_h: 6, p80_h: 16}}

 PWR-003: {implementation: "gen_sch_a.py, gen_sch_e.py, gen_sch_p.py", verification: {tool: none}, maturity: OPEN, gap_category: ABSENT,
   note: "two 25 A blades, three 10 A blades, eFuses and an ideal diode, and no coordination study: no fuse curve is on file",
   remediation: {action: "a protection chain study with the fuse curves obtained from the makers", owner: SESSION, depends_on: [ENV-001, BAT-002], p50_h: 8, p80_h: 20}}

 DEC-001: {implementation: "bypass_slots.py, bypass_place.py", verification: {tool: intent_checks.py, verdict: intent_decoupling, fixtures: tests/test_board_gates.py},
   maturity: ENFORCED, gap_category: HEURISTIC_AS_LAW,
   note: "the 3 mm distance is a project number applied to every device; the loop inductance it stands for is never computed, and a board declares exceptions in an allow file",
   remediation: {action: "derive the distance per device class from the current's spectral content; keep the 3 mm as a screen", owner: SESSION, depends_on: [SI-001], p50_h: 6, p80_h: 16}}

 PI-001: {implementation: "power_copper.py, gen_pcb_*3.py bands", verification: {tool: dc_drop.py, verdict: dc_density, fixtures: tests/test_conductor_current.py},
   maturity: SOURCE_UNVERIFIED, gap_category: SOURCE_OR_APPLICABILITY_UNRESOLVED,
   note: "the conductor measure is implemented and tested; its limit comes from a formula in a code comment, no IPC document is in the tree, and IPC-2152 has never been considered.
     16 September 2026: it reads its OWN verdict now (dc_density). One verdict served both power rules, so a density
     miss failed the board through the drop rule and a limit with no source decided a board through a rule that has
     one. Board A read MISSED VBAT 0.38 percent of 14.4 V, which looks like a voltage failure and is a density one",
   remediation: {action: "obtain the standard, pin it under v2/vendor/, cite the clause, and re-judge the four boards", owner: SESSION, depends_on: [], p50_h: 4, p80_h: 12}}

 PI-002: {implementation: "power_copper.py", verification: {tool: dc_drop.py, verdict: dc_drop, fixtures: tests/test_conductor_current.py},
   maturity: ENFORCED, gap_category: NONE,
   note: "the solver's residual is checked before the number is read. Since 16 September the dc_drop verdict carries the
     VOLTAGE DROP alone, against each rail's own budget, and the current capacity is a separate verdict under PI-001"}

 PI-003: {implementation: "prefanout.py, gen_pcb_*3.py stitch vias", verification: {tool: via_current.py, verdict: via_current, fixtures: tests/test_via_current.py},
   maturity: GENERATED_ONLY, gap_category: SOURCE_OR_APPLICABILITY_UNRESOLVED,
   note: "16 September 2026: it is measured now. Each declared rail's vias are grouped into SITES, a cluster of
     that net's barrels within 6 mm, which is what a layer transition looks like on these boards, and the rail's
     peak current is compared against the weakest site, because a transition is a series element. The barrel is
     geometry (an annulus of the plating thickness) and the plating thickness is the FABRICATOR'S published
     18 um, but the curve is IPC-2221's and that document is not in this tree, so the maturity is
     GENERATED_ONLY and the number is a calculation this project made rather than a limit a document gave it.
     It lands where the trade's own rule of thumb lands, one ampere through a 0.3 mm via at 20 K, which is the
     check that the expression has no unit error in it; the record's own '2.5 A per 0.4 mm hole' comment of
     5 September is nearly three times the computed figure and cites nothing. Its first run on real boards found
     something on all seven and most of it is one false shape: a rail's weakest SITE is often a lone stitch via
     at the end of a pour carrying almost none of the current, while the current travels in a band with a via
     field under it (board E's CELL_F reads 18 A through one via). The arithmetic is right and the attribution
     is not, so the verdict is ADVISORY and the rule reads as unverified rather than failed until the per-via
     current comes from dc_drop's solved mesh",
   remediation: {action: "read the per-via current from dc_drop's solved mesh so a lone stitch via at the end of a pour stops being read as the rail's transition, then take this off advisory; and obtain IPC-2221 or a fabricator statement of via ampacity, so the curve behind the number has an authority", owner: SESSION, depends_on: [STK-001, PI-001], p50_h: 6, p80_h: 16}}

 GND-001: {implementation: "gen_pcb_*3.py pours", verification: {tool: check_zone_nets.py, verdict: check_zone_nets, fixtures: tests/test_gate_fixtures.py},
   maturity: GENERATED_ONLY, gap_category: GENERATED_ONLY,
   note: "phantom-net pours are refused and pour coverage is measured; no check asks whether a partition is deliberate or where a crossing happens",
   remediation: {action: "a per-board ground statement and a check that no signal crosses an undeclared split", owner: SESSION, depends_on: [], p50_h: 6, p80_h: 16}}

 GND-002: {implementation: "gen_sch_c.py standoff bond, gen_sch_b.py magnetics termination",
   verification: {tool: "a written strategy and a bench measurement", document: "docs/GROUNDING-AND-SHIELDS.md"},
   maturity: DOCUMENTED_ONLY, gap_category: PROSE_ONLY,
   note: "16 September 2026: the strategy is WRITTEN, which it was not before, and writing it found the gap. The kit has
     no single conductive enclosure: the Peli 1450 is plastic and every piece of metal in it is an island held by
     plastic, so the only thing joining them is whatever this design says joins them. Board B's magnetics termination
     matches the switch vendor's clause exactly except in one respect: the 1 nF 2 kV common-node capacitor and the RJ45
     shell both land on SIGNAL ground, where the clause says chassis, and THERE IS NO CHASSIS NET ANYWHERE IN THIS KIT.
     The four board changes that would make the strategy true are listed in the document; they are schematic changes on
     three boards and they touch owner decision 29's territory, so they wait on that ruling rather than being made
     piecemeal. It stays DOCUMENTED_ONLY because a bond is confirmed by a four-wire measurement and an emissions sweep,
     neither of which exists until there is hardware",
   remediation: {action: "make the four changes the strategy names once decision 29 is answered, then the bond resistances and the pre-compliance sweep at the prototype",
     owner: SESSION, depends_on: [INT-002], p50_h: 4, p80_h: 12}}

 STK-001: {implementation: "stackup_write.py, written LAST in full.sh so it survives the chain",
   verification: {tool: "impedance_check.py, fab_limits.py", verdict: "impedance_check, fab_limits", fixtures: "tests/test_stackup_reader.py, tests/test_fab_limits.py"},
   maturity: ENFORCED, gap_category: NONE,
   note: "16 September 2026, and it took three separate findings to get here. The stackup is DECLARED: it is written
     into the board and the pre-route chain refuses a board whose stackup did not survive, which it had to, because
     boards A and B reached the router carrying none at all while their logs said one was written (the block goes in as
     text and every pcbnew save after it drops it). It is FEASIBLE: fab_limits.py compares the board's own minimums
     against the fabricator's capability for the copper weight the stackup itself carries. And the capability document
     is in the tree at last, fetched and transcribed on 16 September, which is what makes either statement more than an
     assertion. What is still owed and is NOT claimed here is the fabricator's CONFIRMATION of the two specific stacks
     for these boards, which is a question for the order"}

 STK-002: {implementation: NONE_YET, verification: {tool: layer_judge.py, verdict: layer_judge},
   maturity: OWNER_DECISION_REQUIRED, gap_category: SOURCE_OR_APPLICABILITY_UNRESOLVED,
   note: "layer_judge measures what the router did, not what the board needs, and says so; no like-for-like price for four against six layers exists for any board, which decision 2 left open and decisions 27 and 28 now need",
   remediation: {action: "quotes from the ordering session for both counts on C and P, then the two rulings", owner: OWNER, depends_on: [], p50_h: 2, p80_h: 72}}

 RET-001: {implementation: "signal_class.py plus each board's signal_classes table, judged by intent_checks.py",
   verification: {tool: intent_checks.py, verdict: intent_return_path, fixtures: tests/test_signal_class.py},
   maturity: GENERATED_ONLY, gap_category: SOURCE_OR_APPLICABILITY_UNRESOLVED,
   note: "16 September 2026: the principle is implemented and the heuristic is no longer standing in for it. Every net
     gets a spectral-content class from EVIDENCE (the board's own impedance-targeted net class, or a declaration in
     boards/<letter>.json carrying a written basis naming the part or interface that decides it), and each class is asked
     the question it deserves: a fast net for an ADJACENT reference within a tolerance, a slow one for whether a return
     path EXISTS at all. Four properties keep a relaxation from becoming an exemption mechanism, each with a rule:
     a declaration without a reason is refused, an unclassified net is judged at the STRICTEST bar and named, a
     declaration cannot downgrade a net the board itself calls impedance-targeted, and the relaxed class asks a
     different QUESTION rather than a looser number. It is GENERATED_ONLY and not ENFORCED for one reason and it is
     the honest one: THE PER-CLASS TOLERANCES ARE THIS PROJECT'S OWN NUMBERS, not a standard's. The 10 mm or 5 percent
     of the fast classes is the 15 September ruling's figure calibrated on board A's anti-pad rows; the 30 mm or 15
     percent of CLOCKED_DIGITAL is a judgement. Until a source backs them, they are a written screen and say so",
   remediation: {action: "obtain an authoritative basis for the per-class tolerance (a critical-length criterion from the drivers' own edge rates, or a published guideline), and declare the remaining boards' nets the way E's are",
     owner: SESSION, depends_on: [SI-001], p50_h: 8, p80_h: 26}}

 RET-002: {implementation: "gen_pcb_*3.py pours", verification: {tool: intent_checks.py, verdict: intent_return_path, fixtures: tests/test_board_gates.py, report: return_gaps.py},
   maturity: ENFORCED, gap_category: NONE,
   note: "16 September 2026: it no longer stands in for RET-001. The same measurement runs, and what it is compared
     against now depends on the net's declared spectral class, so the screen is a screen again. It still counts a net's
     own via anti-pads as uncovered, which is what RET-003 is about and is why boards D and E read what they read",
   remediation: {action: "measure the anti-pad share of each failing net's gap, so RET-003's question can be separated from this one",
     owner: SESSION, depends_on: [RET-003], p50_h: 3, p80_h: 8}}

 RET-003: {implementation: "gen_pcb_a3.py In2 pours", verification: {tool: ref_change.py, verdict: return_stitch, fixtures: tests/test_ref_change.py},
   maturity: GENERATED_ONLY, gap_category: SOURCE_OR_APPLICABILITY_UNRESOLVED,
   note: "16 September 2026: the reference conductor is determined now, per via, by sampling the fill in a RING
     around the transition (the fill retreats from the via's own barrel, so the centre reads 'no pour' and the
     first version of this measurement came back with every via undetermined on every board). Measured: board A
     86 vias keep their reference, 131 move between two ground planes, 35 move between GND and VBAT because A's
     In2 carries both side by side; C 62 and 166 and none; D 145 and none; E 202 and none. The first two cases
     are RET-004's and are gated there. The third is this rule's and is gated here: a return crossing between
     two DIFFERENT reference nets crosses at a capacitor or it goes the long way round. The distance is declared
     per board (board A: 3 mm, the same number this project already uses for a decoupling capacitor from its
     pin) and it is OURS, so the maturity is GENERATED_ONLY until a document states it",
   remediation: {action: "find a published figure for how far a plane-stitching capacitor may sit from the transition it serves, or measure one", owner: SESSION, depends_on: [RET-001], p50_h: 3, p80_h: 12}}

 RET-004: {implementation: "gnd_grid.py, return_via.py fixer",
   verification: {tool: return_via.py, verdict: return_via, fixtures: "tests/test_board_gates.py, tests/test_signal_class.py"},
   maturity: ENFORCED, gap_category: NONE,
   note: "16 September 2026: it reads the SAME spectral-content class rule 1 reads, so a net cannot be slow for one rule
     and fast for the other. A return via exists so the return current can follow its signal through a reference change;
     where the signal has no edge worth speaking of there is no loop worth closing, and demanding one anyway refuses
     boards for nothing. Board D was one item from a clean gate on fifteen vias, and every one of them is a push-to-talk
     line or an expander output that changes state when a person presses a switch. The skip is a COUNTED category in the
     verdict, not a silent one, and a net nobody classified is judged as though it were fast"}

 IMP-001: {implementation: "gen_pcb_*3.py net classes", verification: {tool: impedance_check.py, verdict: impedance_check, second_opinion: impedance_2d.py},
   maturity: SOURCE_UNVERIFIED, gap_category: SOURCE_OR_APPLICABILITY_UNRESOLVED,
   note: "closed forms from a standard not in the tree; the 2D solver disagrees by 38 percent on the stripline case with one solved point; no fabricator confirmation and no coupon",
   remediation: {action: "solve every geometry, pin the standard, and get the fabricator's written confirmation with a coupon where the tolerance needs it", owner: SESSION, depends_on: [STK-001], p50_h: 8, p80_h: 24}}

 IMP-002: {implementation: "gen_pcb_*3.py class table (board B's project classes are built from CLASSES since 16 September)",
   verification: {tool: class_floor.py, verdict: class_floor, fixtures: "tests/test_board_gates.py, tests/test_return_rules.py"},
   maturity: ENFORCED, gap_category: NONE,
   note: "16 September 2026: board B's project file shipped USB and DIFF100 at 0.10 mm against its own 0.127 mm minimum, a second hand-written copy that had drifted from the generator's table. The project classes come from the table now and full.sh blocks the placed board on class_floor.py"}

 PAIR-001: {implementation: "pair_preroute.py, meander.py", verification: {tool: pair_match.sh, verdict: impedance_check, fixtures: tests/test_pair_leg_match.py},
   maturity: ENFORCED, gap_category: SOURCE_OR_APPLICABILITY_UNRESOLVED,
   note: "coupling and the 1 mm length gate are enforced; the 1 mm is a project number and the interface's own skew budget is not cited beside it",
   remediation: {action: "cite each interface's skew budget and judge against it", owner: SESSION, depends_on: [INT-001], p50_h: 3, p80_h: 8}}

 SI-001: {implementation: NONE_YET, verification: {tool: none}, maturity: OPEN, gap_category: ABSENT,
   note: "no net in this project has ever been classified by edge rate; every impedance and return decision rests on that classification",
   remediation: {action: "a per-driver edge-rate table from the datasheets, the critical length on each stackup, and the net classification", owner: SESSION, depends_on: [], p50_h: 10, p80_h: 30}}

 CLK-001: {implementation: "gen_sch_*.py crystal networks",
   verification: {tool: clock_check.py, verdict: clock_check, fixtures: tests/test_clock_check.py},
   maturity: ENFORCED, gap_category: NONE,
   note: "16 September 2026. Every crystal on every board is judged from the netlist: both signal pins loaded to ground,
     the two load capacitors the SAME value because an unbalanced pair pulls the oscillator off frequency, one series
     damping resistor allowed and no second driving part on the node. Eleven crystals across four boards pass; board A
     carries none. What is NOT judged is the load capacitor's VALUE against each part's own C_L, which lives in a
     datasheet this tool does not read, so the value found is printed for a person to compare. The check shipped with a
     false positive worth recording: it called every series damping resistor a defect, on four boards at once, and board
     D's hub crystal carries the justification in its own value string (Rd 1.5k per SLLS413 figure 6). A guard whose
     condition is a hypothesis about the data, for the sixth time in this project's record",
   remediation: {action: "compare each load capacitor against its crystal's own C_L from the part datasheet, and the straps half of this rule",
     owner: SESSION, depends_on: [], p50_h: 3, p80_h: 8}}

 ANA-001: {implementation: "gen_pcb_p3.py Kelvin sense, gen_sch_d.py mic network", verification: {tool: none}, maturity: GENERATED_ONLY, gap_category: GENERATED_ONLY,
   note: "the shunt's Kelvin connection and the microphone filtering are designed; no check identifies sensitive nodes or measures their separation from switching copper",
   remediation: {action: "declare sensitive nodes in the intent file and check their clearance and reference", owner: SESSION, depends_on: [], p50_h: 6, p80_h: 16}}

 RF-001: {implementation: "gen_pcb_d3.py, gen_pcb_b3.py keep-outs", verification: {tool: check_pcb_d.py, verdict: check_pcb_d},
   maturity: GENERATED_ONLY, gap_category: GENERATED_ONLY,
   note: "the D gate checks the RF chain's net continuity and the module keep-outs; no line impedance is computed, no filter is verified, and the LPF values are flagged for a simulation that has not run",
   remediation: {action: "compute each RF line on its stackup, simulate the LPF, and record the module keep-out check", owner: SESSION, depends_on: [IMP-001], p50_h: 8, p80_h: 24}}

 RF-002: {implementation: "gen_sch_c.py, gen_sch_d.py, gen_sch_a.py", verification: {tool: check_contracts.py, verdict: check_contracts, fixtures: tests/test_gate_fixtures.py},
   maturity: ENFORCED, gap_category: NONE, note: "driver count, sense and fail-safe pull are checked across three boards"}

 INT-001: {implementation: "gen_sch_*.py, with the PCIe clauses of the module datasheet held by check_contracts.py",
   verification: {tool: check_contracts.py, verdict: check_contracts, fixtures: tests/test_gate_fixtures.py},
   maturity: GENERATED_ONLY, gap_category: PROSE_ONLY,
   note: "16 September 2026: the FIRST interface source in this tree was read, the Compute Module 5 datasheet, and it decided
     three things. PCIe on this host is 90 ohm differential with intra-pair match within 0.1 mm, so the class board B already
     assigns is correct and the PCIe wants 85 note of 9 September is withdrawn for this design. Every PCIe receive line needs
     a 220 nF series capacitor before it enters the IC, and board B had none on three slots: six capacitors added at the
     source and a contract now holds the shape (the module's receive net carries exactly one capacitor and never a second
     device). Ethernet is 100 ohm differential, intra-pair within 0.15 mm. This closes ONE interface of several: USB,
     M.2 and HDMI have no sheet, which is why the maturity is GENERATED_ONLY and not ENFORCED",
   remediation: {action: "per-interface sheets for USB, M.2 and HDMI from their own specifications, the way PCIe was done today",
     owner: SESSION, depends_on: [], p50_h: 10, p80_h: 34}}

 INT-002: {implementation: "gen_sch_b.py, eight 100 nF series capacitors per module link, no magnetics",
   verification: {tool: "both vendors' documents, read 16 September 2026", document: "docs/OWNER-DECISIONS-2026-09-11.md"},
   maturity: OWNER_DECISION_REQUIRED, gap_category: SOURCE_OR_APPLICABILITY_UNRESOLVED,
   note: "BOTH ends read on 16 September 2026, and they do not close the question. The switch vendor PERMITS this exact
     topology and board B matches its clause word for word (Microchip DS00004151A section 6.6: transformer-less where the
     PHY-to-PHY connection is within one PCB, a single DC blocking 0.1 uF in series on each of the eight signals, no
     additional components between the switch and the capacitor). The module's datasheet describes ONE topology, a 1:1 RJ45
     MagJack, and never discusses capacitive coupling; and the switch vendor's own clause names the gap in a sentence, the
     other device may require termination or other circuitry. The module's PHY is a Broadcom BCM54210PE and Broadcom does
     not publish its datasheet, so this cannot be closed from any document this project can obtain. Owner decision 29, with
     three costed options; the recommendation is to fit the magnetics board B already carries on its wall port",
   remediation: {action: "owner decision 29: fit magnetics on the three module links, ask the module vendor, or defer to the prototype",
     owner: OWNER, depends_on: [], p50_h: 6, p80_h: 30}}

 TRN-001: {implementation: "gen_sch_*.py protection parts and each board's external_ports declaration",
   verification: {tool: port_protect.py, verdict: port_protect, fixtures: tests/test_port_protect.py},
   maturity: ENFORCED, gap_category: NONE,
   note: "16 September 2026: every conductor that leaves the enclosure is followed from its connector through fuses,
     beads, chokes and resistors until it meets a clamp. Board B passes: its wall Ethernet is behind the H5007NL
     magnetics, which is galvanic isolation and the strongest protection there is. The other four boards carry NINE
     conductors that reach a chip with nothing between, and they are owner decision 31: the USB-C outlet's two
     configuration channels (its VBUS pin is clamped and they are not), four conductors on board C's panel FACE which a
     person touches, two headset microphone lines, the sensor pod's supply through its sealed M8 receptacle, and board
     E's shore inlet whose SMCJ33A sits BEHIND the pass FET so that FET sees the transient the clamp exists to stop.
     The check took four rounds of false-positive removal to get there, each recorded in its own docstring: a part
     family matched with a word boundary that a part number does not have, galvanic isolation not recognised as
     protection, protection treated as a single net rather than a chain, and a search that walked out through a power
     rail and found a clamp on the other side of the board"}

 ISO-001: {implementation: "gen_pcb_a3.py HV class, gen_pcb_b3.py HV class", verification: {tool: none}, maturity: OPEN, gap_category: ABSENT,
   note: "an HV net class widens the clearance on the PoE nets; no creepage or clearance distance has ever been measured on any board, and no pollution degree or material group is declared",
   remediation: {action: "declare the envelope's pollution degree, obtain the spacing table, and measure the PoE and pack nets", owner: OWNER, depends_on: [ENV-001], p50_h: 8, p80_h: 24}}

 THM-001: {implementation: "gen_pcb_*3.py copper and thermal vias", verification: {tool: thermal.py, verdict: thermal, fixtures: tests/test_thermal.py},
   maturity: GENERATED_ONLY, gap_category: SOURCE_OR_APPLICABILITY_UNRESOLVED,
   note: "16 September 2026: the first half exists. thermal.py builds a per-board table from the board's own
     intent (rails, currents, the source part) and from what each board declares: a converter's loss is
     P_out * (1/efficiency - 1) with the efficiency declared PER RAIL and its basis in the note, and a part
     that dissipates for another reason (the power amplifier, a shunt, a linear regulator) is declared in
     boards/<letter>.json with its watts and its sentence. The thermal path is measured off the board, the
     part's own pad area and the vias inside its courtyard, because on a sealed case with no airflow that is
     what carries the heat out. What is MISSING is the second half: a junction temperature needs the envelope's
     maximum ambient, which is ENV-001 and is an owner decision nobody has written, so the rule stays short of
     its acceptance criteria and says which half is absent rather than inventing it. A rail with no declared
     efficiency is listed as unknown and makes the verdict INCONCLUSIVE, so the table is a floor until every
     converter carries its number",
   remediation: {action: "declare an efficiency per rail and the dissipators per board, then take ENV-001's ambient and estimate a junction temperature for each part above the declared threshold", owner: SESSION, depends_on: [ENV-001], p50_h: 6, p80_h: 20}}

 PLC-001: {implementation: "gen_pcb_*3.py, regionfit.py", verification: {tool: "hardset.py, place_audit.py", verdict: "hardset-placed, place_audit", fixtures: "tests/test_region_fit.py, tests/test_board_gates.py"},
   maturity: ENFORCED, gap_category: NONE, note: "the cheap gate runs before the expensive stage and blocks above a declared allowance"}

 PLC-002: {implementation: NONE_YET, verification: {tool: none}, maturity: DOCUMENTED_ONLY, gap_category: PROSE_ONLY,
   note: "the principle is written in the record and in the plan; the finish still carries ten copper-editing passes and the predictor covers two of the classes",
   remediation: {action: "each closer names its class and its prevention attempt; extend the predictor", owner: SESSION, depends_on: [], p50_h: 6, p80_h: 20}}

 RTE-001: {implementation: "gen_pcb_*.py design rules",
   verification: {tool: "drc.sh, fab_limits.py", verdict: "hardset-routed-board-gate, fab_limits", fixtures: tests/test_fab_limits.py},
   maturity: ENFORCED, gap_category: NONE,
   note: "16 September 2026. The DRC enforces the board's own rules and nothing had ever asked whether THOSE RULES are
     inside what the process makes at the board's own copper weight. fab_limits.py asks, against the fabricator's
     capability document now in the tree, with the copper weight read from the board's own stackup rather than from a
     literal. It found board P immediately: two layers at 2 oz by owner ruling 7, designed to 0.127 mm track width and
     clearance where the 2 oz two-layer rows read 0.16, and its Default, SENSE and GNDC classes at 0.127 as well. Five
     items, on a board every gate in this project had passed, because each of them judges the COPPER against the
     board's rules and none of them judged the RULES. Fixed at the source in gen_pcb_p.py and gen_pcb_p3.py.
     Boards A and B come back INCONCLUSIVE because their committed board files carry no stackup at all, which is
     STK-001's problem and is visible here for the first time"}

 RTE-002: {implementation: "routeflow.py, finish.sh", verification: {tool: hardset.py, verdict: "hardset-routed-board-gate, pruned_gate", fixtures: "tests/test_hardset.py, tests/test_gate_fixtures.py"},
   _tool_why: "two tools, and the second is why: escape_prune removes an escape stub that sits in a hard violation and leaves the pad to the router with only a count printed. On a 0.4 mm receptacle row that pad can end with no via site on any layer, and the routed board looks complete because the router simply never reached it (board A19s SDA pin, 8 September 2026)",
   maturity: ENFORCED, gap_category: NONE, note: "fifteen types, two exemptions, denominators, and a report that cannot be read as a pass"}

 VIA-001: {implementation: "escape.py, prefanout.py, gen_pcb_*.py minimums",
   verification: {tool: "via_audit.py, check_pcb_c.py", verdict: "via_audit, check_pcb_c", fixtures: tests/test_board_gates.py},
   maturity: ENFORCED, gap_category: NONE,
   note: "16 September 2026: every via on the board is judged against the BOARD'S OWN minimum diameter and drill, which
     the board carries and the DRC already believes, and the vias that sit inside a pad are counted and named. A via in
     a pad is NOT a defect here: it is a deliberate technique on exposed pads and fine-pitch escapes, and it carries a
     fabrication cost (filled and capped) that the order paperwork has to name, so reporting it is how that note gets
     written. The ANNULAR RING is a separate verdict under VIA-002 because it is judged against a FABRICATOR
     capability and not against the board"}

 VIA-002: {implementation: "escape.py, prefanout.py, gen_pcb_*.py minimums; boards/<letter>.json annular_min_mm and via_ring_min_mm",
   verification: {tool: via_audit.py, verdict: via_annular, fixtures: tests/test_board_gates.py},
   maturity: ENFORCED, gap_category: SOURCE_OR_APPLICABILITY_UNRESOLVED,
   note: "the floor arrived on 16 September with the fabricator's own capability page, and reading it carefully split
     this rule in two: the ANNULAR RING row governs a plated COMPONENT hole, and a VIA is governed by the via rows,
     which leave 0.05 mm of copper per side at that document's own minimum of 0.25 mm on 0.15 mm. Applied the other way
     round it refused board D12, a board at 0 hard and 0 unrouted, for one 0.45/0.20 via at 0.125 mm of ring. A, B, C, D
     and E declare both floors with the clause cited; E5 and P are 2 oz, the annular rows are stated for 1 oz only, and
     they stay INCONCLUSIVE rather than being judged against a number for another process",
   remediation: {action: "ask the fabricator for the annular ring rows AT 2 oz, which its published page does not state, so boards E5 and P can be judged rather than left inconclusive",
     owner: VENDOR, depends_on: [], p50_h: 2, p80_h: 48}}

 PLN-001: {implementation: "pour_stitch.py, zone_pad_via.py, stitch_prune.py", verification: {tool: check_zone_nets.py, verdict: check_zone_nets, fixtures: tests/test_gate_fixtures.py},
   maturity: ENFORCED, gap_category: NONE, note: "isolated copper is a hard type and pour islands are stitched or removed under a guard"}

 EMC-001: {implementation: NONE_YET, verification: {tool: none}, maturity: OPEN, gap_category: ABSENT,
   note: "no EMC analysis of any kind exists; eleven radios and a sealed metal box",
   remediation: {action: "an EMC sheet per board and a pre-compliance plan for the kit", owner: SESSION, depends_on: [ENV-001, SI-001], p50_h: 10, p80_h: 40}}

 MEC-001: {implementation: "panel1450.py, gen_pcb_*.py", verification: {tool: "check_pcb_*.py", verdict: "check_pcb_<letter>", fixtures: tests/test_board_gates.py},
   maturity: ENFORCED, gap_category: NONE, note: "every mechanical number is asserted against the single module that owns it"}

 DFM-001: {implementation: "build_pcb.sh, export_jlc.sh", verification: {tool: verify_deliverable.py, verdict: verify_deliverable, fixtures: tests/test_gate_fixtures.py},
   maturity: ENFORCED, gap_category: NONE, note: "the gerber zip is counted against the board's copper layer count"}

 DFA-001: {implementation: "export_jlc.sh, jlc-rotations.csv", verification: {tool: verify_deliverable.py, verdict: verify_deliverable},
   maturity: GENERATED_ONLY, gap_category: NO_PROTOCOL,
   note: "the CPL's form is checked; the rotation of each polarised footprint has never been verified against its own drawing or the assembler's preview, and the rotation table is inherited",
   remediation: {action: "a per-footprint rotation verification protocol with the assembler's preview as the artefact", owner: SESSION, depends_on: [], p50_h: 4, p80_h: 12}}

 TST-001: {implementation: "rules_render.py bringup_doc, from each board's own intent file",
   verification: {tool: rules_render.py, document: "docs/PCB-BRING-UP.md", fixtures: tests/test_rules_status.py},
   maturity: VERIFIED_MANUALLY, gap_category: NONE,
   note: "16 September 2026. A bring-up sheet per board, GENERATED from what the board itself declares, so it cannot
     describe a rail the board does not have and cannot miss one it does. An INPUT is applied from a bench supply
     current-limited to the rail's own typical current; a DERIVED rail is never applied, its step is a measurement, and
     a reading outside plus or minus 5 percent stops the sequence. The order is by DEPENDENCY and not by voltage: the
     first draft sorted by voltage ascending and would have had a supply put 3.3 V onto board A's logic rail before the
     14.4 V pack node feeding the converter that makes it. Where a rail's declared source is an inductor or a ferrite
     the tool says it CANNOT TELL a filter choke on an incoming feed from a converter's output, and lists those
     separately to be decided before powering, rather than choosing. It is a procedure and a person does it, which is
     why this is VERIFIED_MANUALLY and the record is the page"}

 REL-001: {implementation: NONE_YET, verification: {tool: none}, maturity: OPEN, gap_category: ABSENT,
   note: "nothing considers vibration, mating cycles or moisture for any interface",
   remediation: {action: "a reliability sheet per board and the test plan for the prototype", owner: OWNER, depends_on: [ENV-001], p50_h: 6, p80_h: 20}}

 BAT-001: {implementation: "gen_sch_p.py BQ4050 and protection FETs", verification: {tool: none}, maturity: GENERATED_ONLY, gap_category: GENERATED_ONLY,
   note: "the protection is designed and its thresholds are configured in software; no check compares a threshold against the cell's own limits, and the cell is not chosen",
   remediation: {action: "the cell's datasheet on file, the threshold table derived from it, and the prototype protection test", owner: OWNER, depends_on: [ENV-001], p50_h: 6, p80_h: 20}}

 BAT-002: {implementation: "gen_sch_a.py, gen_sch_e.py, gen_pcb_e5.py, gen_sch_p.py", verification: {tool: none}, maturity: OPEN, gap_category: ABSENT,
   note: "the chain crosses four boards with two 25 A blades and 12 AWG wiring, and no document draws it end to end",
   remediation: {action: "the chain diagram with ratings and prospective fault current at each stage", owner: SESSION, depends_on: [PWR-003], p50_h: 4, p80_h: 12}}

 DOC-001: {implementation: "full.sh PHASE, silk_fix_all.py", verification: {tool: final_gate.py, verdict: "final_gate_<letter>", fixtures: tests/test_final_gate.py},
   maturity: ENFORCED, gap_category: NONE, note: "added 16 September: a folder that is not the declared phase fails the set, with a truth-table fixture"}

 DOC-002: {implementation: "ledger.py, provenance.json", verification: {tool: ledger_verify.py, verdict: ledger_verify, fixtures: tests/test_ledger.py},
   maturity: GENERATED_ONLY, gap_category: GENERATED_ONLY,
   note: "the ledger is tamper-evident and verified; the appendix's numbers are not machine-linked to the artefacts they came from",
   remediation: {action: "evidence ids in the record entries, emitted by the same writer", owner: SESSION, depends_on: [SGN-001], p50_h: 4, p80_h: 12}}

 OUT-001: {implementation: "make_handoff.py", verification: {tool: final_gate.py, verdict: "final_gate", fixtures: tests/test_final_gate.py},
   maturity: ENFORCED, gap_category: DUPLICATED_OR_STALE,
   note: "the gate refuses a stale or quote-only set correctly; the set on disk is built from folders three to five phases behind and has not been rebuilt",
   remediation: {action: "rebuild the order set from the current folders once the boards are cut", owner: SESSION, depends_on: [DOC-001], p50_h: 2, p80_h: 6}}

 SGN-001: {implementation: "rules_status.py, over pcb_rules.yaml and pcb_rules_coverage.yaml",
   verification: {tool: rules_status.py, verdict: rules_complete, fixtures: tests/test_rules_status.py},
   maturity: ENFORCED, gap_category: NONE,
   note: "16 September 2026. The verdict read here is rules_complete and NOT rules_status, because the two answer different
     questions: rules_status says whether the boards pass, rules_complete says whether every rule that applies to a board
     reached one of the five results at all. The second is the property this registry exists for, and it is the one that
     would otherwise be invisible, since a rule missing from the table looks exactly like a rule that does not apply. It is
     FAIL when any pair produces a result outside the five, when the registry fails its own validator, or when an applied
     rule has no coverage entry; 294 of 294 pairs resolve today"}

 SGN-002: {implementation: "rules_render.py prototype_doc", verification: {tool: rules_render.py, document: docs/PCB-PROTOTYPE-UNKNOWNS.md, fixtures: tests/test_rule_gate_mapping.py},
   maturity: VERIFIED_MANUALLY, gap_category: NONE,
   note: "16 September 2026: v2/docs/PCB-PROTOTYPE-UNKNOWNS.md is generated from the registry's verification phase and methods, so the list cannot quietly shrink; rules_render --check refuses a hand-edited copy and the suite runs it"}
